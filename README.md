# Async Task Service

Небольшой backend-сервис на Go для асинхронной обработки задач через очередь.

## Что умеет

Сервис позволяет:

- создавать задачи через HTTP API
- получать задачу по id
- смотреть список всех задач
- обрабатывать задачи в фоне с помощью worker'а
- менять статус задачи в процессе обработки

Поддерживаемые статусы задачи:

- `pending`
- `processing`
- `done`
- `failed`

## Стек

- Go
- Gin
- PostgreSQL
- pgx
- Docker
- Docker Compose

## Архитектура

Проект разделён на несколько слоёв:

- `handler` — HTTP-обработчики
- `service` — бизнес-логика
- `repository` — работа с базой данных
- `worker` — фоновая обработка задач
- `model` — структуры данных
- `config` — конфигурация приложения

### Как работает обработка задач

1. Клиент создаёт задачу через `POST /tasks`
2. Задача сохраняется в БД со статусом `pending`
3. Фоновый worker периодически ищет `pending` задачи
4. Найденная задача переводится в `processing`
5. После обработки задача получает статус:
    - `done`, если выполнена успешно
    - `failed`, если произошла ошибка

Для безопасного захвата задач используется PostgreSQL-механизм:

`FOR UPDATE SKIP LOCKED`

Это позволяет нескольким worker'ам не брать одну и ту же задачу одновременно.

## Структура проекта

```text
.
├── cmd/
│   └── app/
│       └── main.go
├── internal/
│   ├── config/
│   ├── handler/
│   ├── model/
│   ├── repository/
│   ├── service/
│   └── worker/
├── migrations/
│   └── 001_init.sql
├── .env
├── docker-compose.yml
├── Dockerfile
├── go.mod
└── README.md